# FuelTrack Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret for Role-Based Access Control
JWT_SECRET=fueltrack-super-secret-peso-key-2026

# Database Configuration (Defaults to embedded high-performance PostgreSQL via PGlite)
# Optional: Set DATABASE_URL to connect to external PostgreSQL instance
# DATABASE_URL=postgresql://postgres:password@localhost:5432/fueltrack
